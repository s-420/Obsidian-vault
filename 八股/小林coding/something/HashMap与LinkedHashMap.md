`HashMap` 和 `LinkedHashMap` 之间的关系，可以用八个字完美概括：血缘继承、结构增强。

它们之间的核心关系可以总结为以下三点：

## 1. 它是它的“亲儿子”（继承关系）

在 Java 源码中，`LinkedHashMap` 是直接继承自 `HashMap` 的：

```java
public class LinkedHashMap<K,V> extends HashMap<K,V>
```

这意味着 `HashMap` 拥有的所有基本核心功能（如 Hash 算法、扩容机制、容量大小、加载因子等），`LinkedHashMap` 100% 原封不动地继承了过来。

## 2. 货架与红绳（结构关系）

`LinkedHashMap` 并没有推翻 `HashMap` 的底层设计，而是在其基础之上做加法：

- `HashMap`：底层是数组 +（链表 / 红黑树）。它像一堆杂乱摆放在货架上的商品，只追求查得快（时间复杂度 O(1)），但元素之间是无序的。
- `LinkedHashMap`：内部完完整整地保留了上述货架。唯一的区别是，它在每个节点上额外增加了 `before` 和 `after` 指针，像一根“标记绳”一样，按照插入顺序（或访问顺序）把货架上所有的商品串联了起来。

## 3. 核心性能与特性的妥协（对比关系）

|特性 / 维度|`HashMap`|`LinkedHashMap`|
|---|---|---|
|元素是否有序|无序（遍历顺序和插入顺序无关）|绝对有序（默认按插入顺序，可配置按访问顺序）|
|内存占用|较小|稍大（因为每个节点要多存两个指针的空间）|
|查找性能 (`get`)|极快 (O(1))|极快 (O(1))，与 HashMap 几乎没有区别|
|遍历性能 (`loop`)|慢（需要扫描整个数组及内部所有红黑树）|快（直接沿着标记绳顺藤摸瓜，扫描的全部是有效元素）|
|核心应用场景|普通的键值对高速缓存、去重定位|需要保证顺序的场景、实现 LRU 缓存淘汰算法|

简而言之，`LinkedHashMap` 就是一个穿了顺序红绳、能保证有序的 `HashMap`。

---
