清理策略实现
~~~ java 
/**  
 * 文本清理服务  
 * 提供统一的文本内容清理和规范化功能  
 */  
@Service  
public class TextCleaningService {  
  
    // ========== 预编译正则表达式（性能优化）==========  
    /**  
     * 图片文件名行：image123.png  
     * 整行匹配，防止误删正文中的文件名字符串  
     */  
    private static final Pattern IMAGE_FILENAME_LINE =  
            Pattern.compile("(?m)^image\\d+\\.(png|jpe?g|gif|bmp|webp)\\s*$");  
  
    /**  
     * HTTP/HTTPS 图片链接  
     * 支持 URL 查询参数，大小写不敏感  
     */  
    private static final Pattern IMAGE_URL =  
            Pattern.compile("https?://\\S+?\\.(png|jpe?g|gif|bmp|webp)(\\?\\S*)?", Pattern.CASE_INSENSITIVE);  
  
    /**  
     * 文件协议 URL（Tika PDF 临时文件路径等）  
     */  
    private static final Pattern FILE_URL =  
            Pattern.compile("file:(//)?\\S+", Pattern.CASE_INSENSITIVE);  
  
    /**  
     * 分隔线：---, ___, ***, ===  
     * 整行匹配，至少 3 个连续符号  
     */  
    private static final Pattern SEPARATOR_LINE =  
            Pattern.compile("(?m)^\\s*[-_*=]{3,}\\s*$");  
  
    /**  
     * 控制字符（不可见字符）  
     * 保留换行符 \n (0x0A) 和制表符 \t (0x09)  
     */    private static final Pattern CONTROL_CHARS =  
            Pattern.compile("[\\u0000-\\u0008\\u000B\\u000C\\u000E-\\u001F]");  
  
    /**  
     * HTML 标签  
     */  
    private static final Pattern HTML_TAGS =  
            Pattern.compile("<[^>]+>");  
  
    /**  
     * 清理和规范化文本内容  
     *   
* <p>语义级过滤（简历场景化）：</p>  
     * <ul>  
     *   <li>去除控制字符</li>  
     *   <li>去除图片文件名（整行匹配）</li>  
     *   <li>去除图片链接</li>  
     *   <li>去除文件协议路径</li>  
     *   <li>去除符号分隔线</li>  
     * </ul>  
     *   
* <p>格式级清理：</p>  
     * <ul>  
     *   <li>规范化换行符</li>  
     *   <li>去除行尾空格，保留空行（保持段落结构）</li>  
     *   <li>压缩连续空行（最多保留 2 个换行符）</li>  
     * </ul>  
     *   
* <p>作为 RAG/AI 分析前的"保险层"，确保文本质量</p>  
     *  
     * @param text 原始文本  
     * @return 清理后的文本  
     */  
    public String cleanText(String text) {  
        if (text == null || text.isBlank()) {  
            return "";  
        }  
  
        String t = text;  
  
        // ========== 第一层：语义去噪 ==========        t = CONTROL_CHARS.matcher(t).replaceAll("");  
        t = IMAGE_FILENAME_LINE.matcher(t).replaceAll("");  
        t = IMAGE_URL.matcher(t).replaceAll("");  
        t = FILE_URL.matcher(t).replaceAll("");  
        t = SEPARATOR_LINE.matcher(t).replaceAll("");  
  
        // ========== 第二层：格式规范化 ==========        // 统一换行符  
        t = t.replace("\r\n", "\n").replace("\r", "\n");  
  
        // 去掉行尾空格和制表符，保留空行（保持段落结构）  
        t = t.replaceAll("(?m)[ \t]+$", "");  
          
        // 压缩连续空行：最多保留 2 个换行符（即一个空行）  
        t = t.replaceAll("\\n{3,}", "\n\n");  
  
        return t.strip();  
    }  
  
    /**  
     * 清理文本并限制最大长度  
     *  
     * @param text      原始文本  
     * @param maxLength 最大长度  
     * @return 清理后的文本（可能被截断）  
     */  
    public String cleanTextWithLimit(String text, int maxLength) {  
        String cleaned = cleanText(text);  
        if (cleaned.length() > maxLength) {  
            return cleaned.substring(0, maxLength);  
        }  
        return cleaned;  
    }  
  
    /**  
     * 清理文本并移除所有换行符（转为空格）  
     * 适用于需要单行显示的场景  
     *  
     * @param text 原始文本  
     * @return 单行文本  
     */  
    public String cleanToSingleLine(String text) {  
        if (text == null || text.isBlank()) {  
            return "";  
        }  
  
        return text  
            .replaceAll("[\\r\\n]+", " ")  
            .replaceAll("\\s+", " ")  
            .strip();  
    }  
  
    /**  
     * 移除 HTML 标签和常见 HTML 实体  
     *  
     * @param text 可能包含 HTML 的文本  
     * @return 纯文本  
     */  
    public String stripHtml(String text) {  
        if (text == null || text.isBlank()) {  
            return "";  
        }  
  
        return HTML_TAGS.matcher(text).replaceAll(" ")  
            .replace("&nbsp;", " ")  
            .replace("&amp;", "&")  
            .replace("&lt;", "<")  
            .replace("&gt;", ">")  
            .replace("&quot;", "\"")  
            .replace("&apos;", "'")  
            .replaceAll("\\s+", " ")  
            .strip();  
    }  
}
~~~

## TextCleaningService 文本清理流程概览图

```
┌─────────────────────────────────────────────────────────────────┐
│                    TextCleaningService                          │
│                  文本清理和规范化服务                            │
└─────────────────────────────────────────────────────────────────┘
                              ↓
                    ┌─────────────────┐
                    │   输入：原始文本   │
                    │  (从文档解析而来)  │
                    └─────────────────┘
                              ↓
        ┌─────────────────────────────────────────┐
        │         第一层：语义去噪                 │
        │  (去除无意义的干扰内容)                   │
        └─────────────────────────────────────────┘
                              ↓
    ┌───────────┬───────────┬───────────┬───────────┬───────────┐
    ↓           ↓           ↓           ↓           ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ 去除    │ │ 去除    │ │ 去除    │ │ 去除    │ │ 去除    │
│ 控制字符 │ │ 图片文件名│ │ 图片链接 │ │ 文件路径 │ │ 分隔线  │
└─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
    ↓           ↓           ↓           ↓           ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ 不可见  │ │image123 │ │https:// │ │file://  │ │ ---     │
│ 字符    │ │.png     │ │xxx.png  │ │xxx.pdf  │ │ ___     │
└─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
                              ↓
        ┌─────────────────────────────────────────┐
        │         第二层：格式规范化               │
        │  (统一格式，保持段落结构)                 │
        └─────────────────────────────────────────┘
                              ↓
    ┌───────────┬───────────┬───────────┐
    ↓           ↓           ↓           ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ 统一    │ │ 去除    │ │ 压缩    │ │ 去除    │
│ 换行符  │ │ 行尾    │ │ 连续    │ │ 首尾    │
│         │ │ 空格    │ │ 空行    │ │ 空格    │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
    ↓           ↓           ↓           ↓
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ \r\n→\n │ │ "abc "  │ │ \n\n\n  │ │ " abc " │
│ \r→\n   │ │ →"abc"  │ │ →\n\n   │ │ →"abc"  │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
                              ↓
                    ┌─────────────────┐
                    │   输出：清理文本  │
                    │  (规范、干净)     │
                    └─────────────────┘
```

---

## 详细清理步骤图

### 第一层：语义去噪

```
原始文本
    ↓
┌─────────────────────────────────────────────────────┐
│ 1. 去除控制字符                                        │
│    [0x00-0x08, 0x0B, 0x0C, 0x0E-0x1F]               │
│    保留：\n (换行), \t (制表符)                      │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 2. 去除图片文件名（整行匹配）                          │
│    image123.png                                      │
│    image456.jpg                                      │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 3. 去除图片链接                                       │
│    https://example.com/image.png                     │
│    http://site.com/photo.jpg?size=large             │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 4. 去除文件协议路径                                   │
│    file:///C:/temp/image.png                         │
│    file:/tmp/document.pdf                            │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 5. 去除符号分隔线                                     │
│    ---                                               │
│    ___                                               │
│    ***                                               │
│    ===                                               │
└─────────────────────────────────────────────────────┘
```

---

### 第二层：格式规范化

```
语义去噪后的文本
    ↓
┌─────────────────────────────────────────────────────┐
│ 1. 统一换行符                                         │
│    \r\n → \n (Windows)                              │
│    \r   → \n (旧版 Mac)                             │
│    \n   → \n (Unix/Linux)                           │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 2. 去除行尾空格和制表符                               │
│    "张三   " → "张三"                                │
│    "Java\t" → "Java"                                │
│    （保留空行，保持段落结构）                         │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 3. 压缩连续空行                                       │
│    \n\n\n → \n\n (最多保留一个空行)                 │
│    \n\n\n\n → \n\n                                  │
│    （保持段落分隔，避免过多空行）                      │
└─────────────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────────────┐
│ 4. 去除首尾空格                                       │
│    "  张三  " → "张三"                               │
└─────────────────────────────────────────────────────┘
```

---

## 清理前后对比示例

### 示例 1：简历文本

```
【清理前】
张三\u0000\u0001Java开发工程师
image123.png
https://example.com/photo.jpg
---
教育背景：
清华大学


工作经历：
阿里巴巴

【清理后】
张三
Java开发工程师

教育背景：
清华大学

工作经历：
阿里巴巴
```

---

### 示例 2：技术文档

```
【清理前】
Spring Boot\u000B是一个快速开发框架\r\n
file:///C:/temp/doc.pdf\r\n\r\n\r\n
核心特性：\t\t
___\r\n
1. 自动配置\r\n
2. 起步依赖

【清理后】
Spring Boot是一个快速开发框架

核心特性：

1. 自动配置
2. 起步依赖
```

---

## 其他清理方法

### 1. `cleanTextWithLimit` - 清理并限制长度

```
原始文本 → 清理 → 截断到最大长度 → 返回
```

---

### 2. `cleanToSingleLine` - 清理为单行

```
原始文本 → 替换换行符为空格 → 压缩空格 → 返回单行文本
```

---

### 3. `stripHtml` - 去除 HTML 标签

```
HTML 文本 → 去除标签 → 替换实体 → 压缩空格 → 返回纯文本
```

---

## 清理目的

### 为什么需要清理？

| 问题 | 影响 | 解决方案 |
|------|------|----------|
| 控制字符 | 干扰 AI 分析 | 去除 |
| 图片引用 | 无意义内容 | 去除 |
| 文件路径 | 泄露临时路径 | 去除 |
| 分隔线 | 干扰语义理解 | 去除 |
| 格式混乱 | 影响可读性 | 规范化 |
| 过多空行 | 浪费 Token | 压缩 |

### 清理后的好处

- ✅ **提高 AI 分析准确性**：去除干扰内容
- ✅ **节省 Token 成本**：压缩冗余内容
- ✅ **保持段落结构**：保留语义分隔
- ✅ **统一格式**：便于后续处理

---

## 总结

```
TextCleaningService 清理流程：

输入 → 语义去噪 → 格式规范化 → 输出
       ↓            ↓            ↓
    去除干扰     统一格式     干净文本
    提高质量     保持结构     便于分析
```