# 2026-06-05 每日复盘

## 今日完成

### 1. Spring Task 定时任务实现

**任务**：编写 Spring Task 定时任务，定期检查 learning_lesson 表中的课程是否过期，如果过期则将课程状态修改为已过期。

**完成内容**：
- 在 `ILearningLessonService` 接口中添加 `updateExpiredLessons()` 方法
- 在 `LearningLessonServiceImpl` 中实现 `updateExpiredLessons()` 方法
- 创建 `LessonExpirationTask` 定时任务类

**关键代码**：

```java
@Slf4j
@Component
@RequiredArgsConstructor
public class LessonExpirationTask {

    private final ILearningLessonService lessonService;

    @Scheduled(fixedDelay = 3600000)
    public void checkExpiredLessons() {
        log.debug("开始检查过期课程");
        try {
            lessonService.updateExpiredLessons();
            log.debug("过期课程检查完成");
        } catch (Exception e) {
            log.error("检查过期课程时发生异常", e);
        }
    }
}
```

**技术要点**：
- 使用 `@Scheduled(fixedDelay = 3600000)` 实现每小时执行一次
- 使用 `fixedDelay` 而非 `fixedRate`，避免任务重叠
- 必须捕获异常，避免任务中断
- 批量更新数据库，提高性能

### 2. jdtls 安装配置

**任务**：安装 Eclipse JDT Language Server，用于 Java 代码静态分析。

**完成内容**：
- 下载并解压 jdtls 到 `D:\tools\jdtls`
- 配置 Java 21 环境（jdtls 需要 Java 21+）
- 将 jdtls 添加到 PATH 环境变量

**配置命令**：
```powershell
# 下载 jdtls
Invoke-WebRequest -Uri "https://download.eclipse.org/jdtls/snapshots/jdt-language-server-latest.tar.gz" -OutFile "D:\tools\jdtls\jdtls.tar.gz"

# 解压
cd D:\tools\jdtls; tar -xzf jdtls.tar.gz

# 添加到 PATH
[Environment]::SetEnvironmentVariable("Path", "D:\tools\jdtls\bin;" + [Environment]::GetEnvironmentVariable("Path", "User"), "User")
```

## 知识沉淀

- 创建了 [[Spring Task 定时任务]] 技术笔记
- 记录了 Spring Task 的核心注解、执行规则、实际案例

## 明日计划

- [ ] 测试定时任务是否正常工作
- [ ] 验证 jdtls 在 OpenCode 中的使用
- [ ] 继续 learning 模块的其他功能开发

## 踩坑记录

### NullPointerException - queryOldRecord 传入 null

**问题**：`queryOldRecord` 方法在数据库中不存在记录时，没有 `return null`，导致后续代码将 `null` 传入 `writeRecordCache`，引发 NPE。

**根本原因**：
1. 方法职责混乱：查询方法不应该写入缓存
2. 缺少 null 检查：没有在 record 为 null 时提前返回
3. 防御性编程不足：`writeRecordCache` 没有对入参进行 null 检查

**修复**：在 `queryOldRecord` 中，当 record 为 null 时直接返回，不执行后续的缓存写入操作。

**经验教训**：
- 方法职责要单一，查询方法只负责查询
- 防御性编程，对入参进行 null 检查
- 注意代码逻辑完整性，if 分支要考虑所有情况
- 单元测试可以提前发现这类问题

详见：[[NullPointerException - queryOldRecord 传入 null]]

## 问题与思考

1. **Spring Task vs Quartz**：Spring Task 适合简单场景，Quartz 适合复杂调度需求
2. **定时任务线程池配置**：高并发场景需要配置线程池，默认单线程可能成为瓶颈
3. **jdtls 版本兼容性**：jdtls 需要 Java 21+，与项目使用的 JDK 17 不同，需要注意环境隔离
4. **方法职责单一原则**：查询方法不应该有副作用（如写入缓存），应该让调用方决定后续操作

## 相关链接

- [[Spring Task 定时任务]]
- [[天机学堂项目架构]]
- [[Spring Boot CRUD Patterns]]

---

*创建时间：2026-06-05 14:30*
